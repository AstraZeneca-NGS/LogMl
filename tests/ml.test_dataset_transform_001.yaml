logger:

dataset:
    dataset_name: 'test_dataset_transform_001'
    dataset_path: 'tests/data'
    dataset_type: 'df'
    is_use_default_split: False
    outputs: ['y']

dataset_transform:
  enable: true

dataset_preprocess:
  enable: true
  normalize:
    standard: true

model:
  enable: false
  model_name: 'test_model_dataset_transform_001'
  model_path: 'tests/tmp/test_model_dataset_transform_001'
