logger:

dataset:
  dataset_name: 'test_dataset_augment_001'
  dataset_path: 'tests/unit/data'
  dataset_type: 'df'
  is_use_default_split: False

dataset_explore:
  enable: false

dataset_preprocess:
  enable: false

dataset_transform:
  enable: false

dataset_augment:
  enable: true
  pca:
    pca_x:
      num: 2
      fields: ['x.*']

dataset_feature_importance:
  enable: false
