logger:

dataset:
    dataset_name: 'test_dataset_transform_003'
    dataset_path: 'tests/unit/data'
    dataset_type: 'df'
    is_use_default_split: False
    outputs: ['y']

dataset_transform:
  enable: true
  remove_columns: ['x1', 'd1']

dataset_preprocess:
  enable: false
  normalize:
    standard: true

model:
  enable: false
  model_name: 'test_model_dataset_transform_003'
  model_path: 'tests/tmp/test_model_dataset_transform_003'
