logger:

dataset:
    dataset_name: 'test_dataset_preprocess_010'
    dataset_path: 'tests/unit/data'
    dataset_type: 'df'
    is_use_default_split: False
    outputs: ['y']

dataset_transform:
  enable: true
  shuffle: true

dataset_preprocess:
  enable: false

model:
  enable: false
  model_name: 'test_model_dataset_preprocess_010'
  model_path: 'tests/tmp/test_model_dataset_preprocess_010'
